{
  "name": "color-aware-menu",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "rm -rf ./docs && parcel build src/index.html --public-url /color-aware-menu-poc --dist-dir ./docs && touch ./docs/.nojekyll"
  },
  "author": "Stanko",
  "license": "MIT",
  "dependencies": {
    "html-to-image": "^1.9.0",
    "html2canvas": "^1.4.1"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.4.1",
    "parcel": "^2.4.1",
    "process": "^0.11.10"
  }
}
